import type { FC } from 'react';

export const LoginPage: FC = () => {
  return (
    <div className="p-8 text-center">
      <h1 className="text-2xl font-bold font-heading text-textPrimary">Login Page</h1>
      <p className="text-textSecondary mt-2">Placeholder for Screen 1 — Login</p>
    </div>
  );
};

export default LoginPage;
